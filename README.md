# REM_gui
This is a repository to visualize a relative elevation model above a river using the code that was developed by Open Topography, but using a GUI wrapper to make it easier to use as an executable.
