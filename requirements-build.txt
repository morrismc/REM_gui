# REM GUI - Build Requirements
#
# Install these in a clean virtual environment before building the executable
#
# RECOMMENDED: Use mamba for geospatial dependencies (faster than conda)
#   mamba create -n rem_gui python=3.10
#   mamba activate rem_gui
#   mamba install -c conda-forge gdal geopandas osmnx scipy matplotlib seaborn
#   pip install -r requirements-build.txt
#   pyinstaller rem_gui.spec

# Runtime dependencies (minimal set)
riverrem>=0.2.0

# PyInstaller for building executable
pyinstaller>=5.0

# Optional: UPX for better compression (install separately)
# Download from: https://github.com/upx/upx/releases
